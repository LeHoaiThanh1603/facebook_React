const images = {
    logo: require('../image/logo.svg').default,
    messIcon: require('../image/messageIcon.svg').default,
    bellIcon: require('../image/bellicon.svg').default
};

export default images;